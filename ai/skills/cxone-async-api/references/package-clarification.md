# Package Clarification

* The name `cxone-async-api` is the name of the repository and general
  human-oriented readable name of this project.
* The package, once installed, is imported using the `cxone_api`
  namespace.
